# HotKeys
    #Keyboard layout toggle
shift + alt + space
    python /home/sammy/Scripts/python/toggle_keyboard.py
super + r
    rofi -show drun

    #Apps
alt + shift + b
     firefox
alt + shift + s 
     spotify
alt + shift + i 
     idle
    # Open File manager
alt + shift + f 
     thunar

alt + shift + l 
     betterlockscreen -l


# Brightness
XF86MonBrightnessUp
     brightness.sh up

XF86MonBrightnessDown
     brightness.sh down

# Audio
XF86AudioMute
     amixer sset Master mute

XF86AudioRaiseVolume
     volume.sh up
XF86AudioLowerVolume
     volume.sh down

# Power Off
# PowerOff
# │    eww -c $HOME/.config/eww/Powermenu/ open-many --toggle background background-closer buttons-exit profile-exit quotes-exit cl │ ose-exit
#eww Dashboard
# ctrl + alt + period
     # exec $HOME/.config/eww/dashboard/launch_dashboard
     # eww -c $HOME/.config/eww/Main/ open-many --toggle background system fetch dots spotify power applications links profile clock weather bluetooth mail karma
     



# Open Keybindings Help
shift + alt + h
     sxhkd-keys
